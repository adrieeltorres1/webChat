### 📦 Instalação (passo a passo para iniciantes)

Essas instruções mostram como abrir o projeto no seu computador mesmo que você nunca tenha programado antes. Vai levar só alguns minutos.

**Pré-requisitos simples**
- Ter o Node.js e o npm instalados no computador.  
  - Se não tiver, peça para alguém instalar ou baixe em: https://nodejs.org/ (escolha a versão LTS).
- Ter o código do projeto no seu computador (pasta do repositório). Se não souber como clonar, peça ajuda para copiar a pasta do projeto para o seu computador.

**Passo a passo**

1. Abra o programa **Terminal** (no Windows pode ser o `PowerShell` ou `Prompt de Comando`, no macOS o `Terminal`).
2. No terminal, entre na pasta do projeto. Exemplo (substitua `caminho/para/pasta` pelo local correto):
   ```bash
   cd caminho/para/pasta-do-webchat

```bash
**Execute os dois comandos abaixo um após o outro**
npm install 
npm run dev




